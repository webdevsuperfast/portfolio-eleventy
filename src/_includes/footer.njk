<footer class="fixed w-full bottom-4 lg:bottom-24">
  <div class="container px-4 mx-auto sm:px-6 lg:px-8">
    <div class="relative" x-data="{ open: false }" @click.away="open = false">
      <button x-bind:class="open ? 'rounded-tr-none rounded-tl-none' : ''"
  class="absolute bottom-0 right-0 p-5 bg-gray-700 rounded-br-none hover:cursor-pointer rounded-xl dark:bg-gray-800 hover:bg-gray-400 dark:hover:bg-gray-700 active:bg-gray-400 dark:active:bg-gray-700"
        x-on:click="open = ! open" aria-label="Social Media Links"
      >
        <div class="relative flex items-center justify-center w-5 h-5">
          <span
            x-bind:class="open ? 'rotate-45' : ''"
            class="absolute h-0.5 w-full transform rounded-sm bg-white transition"
          ></span>
          <span
            x-bind:class="open ? 'rotate-45' : ''"
            class="absolute h-full w-0.5 transform rounded-sm bg-white transition"
          ></span>
        </div>
      </button>
      <div
        :class="{ 'block rounded-br-none': open, 'hidden': ! open }"
  class="absolute right-0 hidden overflow-hidden bg-gray-700 bottom-[60px] rounded-xl dark:bg-gray-800"
        x-show="open"
        x-transition
        x-cloak
      >
        {% if social %}
          <div class="">
            {% for item in social %}
              <a
            class="block px-5 py-3 text-center text-white uppercase transition-all hover:bg-gray-400 dark:hover:bg-gray-700 lg:py-5 lg:px-10 lg:text-xl"
            href="{{ item.url }}"
            target="{{ item.target }}"
          >
                {{ item.label }}
              </a>
            {% endfor %}
          </div>
        {% endif %}
      </div>
    </div>
  </div>
</footer>
<!-- End Footer -->